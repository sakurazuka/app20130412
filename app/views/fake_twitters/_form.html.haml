= simple_form_for @fake_twitter, :html => { :class => 'form-horizontal' } do |f|
  = f.input :tweet
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), fake_twitters_path, :class => 'btn'
